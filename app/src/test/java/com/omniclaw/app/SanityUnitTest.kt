package com.omniclaw.app

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Smoke unit test — verifies the test runner is wired up correctly.
 * Real tests should live alongside the modules they cover (e.g. AgentLoopTest,
 * LlmClientTest, SkillRepositoryTest).
 */
class SanityUnitTest {

    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
