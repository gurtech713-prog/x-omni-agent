<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network for LLM calls -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Foreground service for agent loop & scheduled automation -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PROJECTION" />
    <!-- V-M6: required for OverlayService's microphone capture (push-to-talk
         bubble) on Android 14+. Without this permission the FGS is rejected
         at startForeground() with SecurityException when the service type
         includes `microphone`. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- REMOVED: RECEIVE_BOOT_COMPLETED — there is no BOOT_COMPLETED receiver
         declared anywhere in the manifest, so this permission is unused. -->

    <!-- Multimodal perception: camera + microphone -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Optional hardware acceleration for LiteRT on-device inference.
         Not required — LiteRT falls back to CPU if NNAPI/GPU are unavailable. -->
    <uses-feature android:name="android.hardware.neuralnetworks" android:required="false" />
    <uses-feature android:glEsVersion="0x00030000" android:required="false" />

    <!-- Media access for gallery memory & capcut skill (Android 13+) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <!-- Legacy storage for older devices -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <!-- REMOVED: MANAGE_EXTERNAL_STORAGE — GalleryScanner uses MediaStore
         queries which don't require all-files-access on Android 10+. This
         broad permission triggers Play Store policy review and is unnecessary. -->

    <!-- Overlay: floating widget / push-to-talk bubble -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- REMOVED: READ_CLIPBOARD — this is NOT a standard Android permission.
         Clipboard access on Android is governed by foreground app status, not
         a manifest permission. Declaring it is a no-op (system ignores unknown
         permissions) and indicates a misunderstanding. -->

    <!-- REMOVED: SCHEDULE_EXACT_ALARM — unused. All scheduling goes through
         WorkManager, not AlarmManager.setExact*, so this permission was dead
         weight (L-02). USE_EXACT_ALARM was already removed earlier (restricted
         on Play Store and likewise unnecessary). -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".OmniApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.XOmniClaw"
        android:enableOnBackInvokedCallback="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="36">

        <!-- Disable WorkManager's default initializer — we use Configuration.Provider
             via HiltWorkerFactory in OmniApplication. -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.XOmniClaw"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode|density">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Deep-link entry point (C-13): handle omniclaw:// URLs and the
                 https://omniclaw.app host so session/skill links open the app.
                 MainActivity forwards VIEW intents to DeepLinkHandler. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="omniclaw" />
            </intent-filter>
            <!-- V-M7: autoVerify so Android 12+ App Links verification succeeds
                 for the https://omniclaw.app host. Without autoVerify, the
                 system treats this as a regular deep link and shows the
                 app-chooser dialog instead of opening X-OmniClaw directly.
                 The omniclaw:// scheme is custom and doesn't need autoVerify. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="omniclaw.app" />
            </intent-filter>
        </activity>

        <!-- Accessibility service: real UI automation (taps, swipes, tree inspection) -->
        <service
            android:name=".service.OmniAccessibilityService"
            android:exported="false"
            android:label="@string/accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Foreground service: keeps the agent loop alive across long tasks -->
        <service
            android:name=".service.AgentForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="On-device agent execution loop for accessibility-driven automation." />
        </service>

        <!-- Overlay service: floating push-to-talk bubble -->
        <!-- V-M6: foregroundServiceType includes `microphone` because the
             overlay captures audio (AudioRecorder) while in the foreground.
             On Android 14+ a FGS that uses the mic MUST declare the
             microphone type or startForeground() throws SecurityException. -->
        <service
            android:name=".service.OverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse|microphone">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Floating push-to-talk overlay bubble for voice-driven agent control." />
        </service>

        <!-- Halo overlay: Dynamic Island-style agent status pill -->
        <service
            android:name=".service.HaloOverlayService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Dynamic-Island-style agent status pill overlay." />
        </service>

        <!-- Screen capture service: MediaProjection for vision fallback -->
        <service
            android:name=".service.ScreenCaptureService"
            android:exported="false"
            android:foregroundServiceType="mediaProjection" />

        <!-- WorkManager SystemForegroundService for Android 14+ expedited/foreground tasks -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:exported="false"
            android:foregroundServiceType="specialUse"
            tools:node="merge">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Scheduled background automation worker tasks." />
        </service>

        <!-- FileProvider for sharing skill files / exported configs / screenshots
             with other apps via content:// URIs (required since API 24 —
             file:// URIs throw FileUriExposedException). -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_provider_paths" />
        </provider>

    </application>
</manifest>
